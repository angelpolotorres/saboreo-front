import styled from 'styled-components';

export const VegetarianTagStyled = styled.div`
  color: var(--green-secondary);
  border-color: var(--green-secondary);
  font-size: 12px;
  border: solid 1px;
  border-radius: 16px;
  padding: 4px 8px;
  margin: 0 8px 0 0;
`;
