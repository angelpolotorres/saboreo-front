import styled from 'styled-components';

export const VeganTagStyled = styled.div`
  color: var(--green-primary);
  border-color: var(--green-primary);
  font-size: 12px;
  border: solid 1px;
  border-radius: 16px;
  padding: 4px 8px;
  margin: 0 8px 0 0;
`;
